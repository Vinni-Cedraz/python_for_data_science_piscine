def count_in_list(list_data, item):
    """Counts occurrences of an item in a list.

    Args:
        list_data (list): The list to search within.
        item: The item to count.

    Returns:
        int: The number of times the item occurs in the list.
    """
    return list_data.count(item)
